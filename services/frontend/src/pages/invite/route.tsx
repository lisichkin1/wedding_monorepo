import { PATHS } from '@shared/config/PATHS';
import { InvitePage } from './Invite';

const route = {
  title: 'Приглашение',
  path: PATHS.INVITE,
  element: <InvitePage />
};

export default route;
